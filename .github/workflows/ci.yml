name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install
        run: python -m pip install --upgrade pip && python -m pip install -e .
      - name: Rule lint
        run: python rules.py lint
      - name: Unit tests
        run: python -m unittest discover -s tests -p "*_test.py" -v
      - name: Golden tests
        run: python tests/golden_test.py
      - name: Syntax check
        run: python -m compileall -q dataval config/Common/knowhow_py tests run.py rules.py promote.py production_audit.py merge_advisory.py
